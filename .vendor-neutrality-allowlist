crates/ctf-server/src/openapi.rs — retains the `x-openai-isConsequential` plugin-manifest extension field, a load-bearing OpenAI plugin protocol key (inside a JSON literal, cannot be renamed without breaking the CTF fixture's plugin manifest).
docs/architecture/landscape.md — competitive landscape; names vendors and products throughout because a landscape cannot be written without naming competitors. Documentation only, no vendor coupling in code or interfaces. Outside `ci/no-vendor-strings.sh` scan paths (which cover only the two OIDC crates), so this entry is a ledger note rather than a gate suppression.

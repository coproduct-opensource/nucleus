[package]
name = "nucleus-client"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Client utilities for signing nucleus node/proxy requests"
repository = "https://github.com/coproduct-opensource/nucleus"
documentation = "https://docs.rs/nucleus-client"
readme = "README.md"
keywords = ["ai", "security", "client", "hmac"]
categories = ["authentication", "development-tools"]

[dependencies]
hmac = { workspace = true }
sha2 = { workspace = true }
hex = { workspace = true }
# Approvals on a Firecracker pod are verified with Ed25519, not HMAC -- see
# `sign_approval_headers_ed25519`.
ed25519-dalek = { workspace = true }
# v4 UUIDs are the per-request nonce that lets an otherwise-identical request
# repeat inside the node's replay window (#1631).
uuid = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

# Optional async drand client dependencies
reqwest = { workspace = true, features = ["rustls-no-provider", "json"], optional = true }
tokio = { workspace = true, features = ["sync"], optional = true }
tracing = { workspace = true, optional = true }

# BLS signature verification for drand beacons
drand-verify = { version = "0.6", optional = true }

[features]
default = []
async-drand = ["reqwest", "tokio", "drand-verify", "tracing"]

[lints]
workspace = true

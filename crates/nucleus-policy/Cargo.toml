[package]
name = "nucleus-policy"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Policy DSL for zero-permission-prompt AI agent authorization"

[dependencies]
glob = "0.3"
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
thiserror = "2"
tracing = "0.1"

[dev-dependencies]
tempfile = "3"

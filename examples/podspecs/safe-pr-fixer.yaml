apiVersion: nucleus/v1
kind: Pod
metadata:
  name: safe-pr-fixer
  namespace: ci
spec:
  work_dir: /workspace
  timeout_seconds: 600
  policy:
    type: profile
    name: safe_pr_fixer
  network:
    policy: package_registries
  credentials:
    env:
      LLM_API_TOKEN: "${LLM_API_TOKEN}"

# Notes

Point-in-time strategy / planning notes kept out of the repo root for
readability. These are not build inputs and nothing in the build, CI, or mdbook
(`docs/`) references them; the *live* docs live under `docs/` (e.g.
`docs/north-star.md`, `docs/verified-claims.md`).

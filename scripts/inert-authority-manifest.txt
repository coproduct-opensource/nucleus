# Inert-authority manifest — witnesses that are ACCEPTED and DROPPED.
#
# Read by `cargo xtask inert-authority` (crates/xtask/src/inert_authority.rs)
# via the shim scripts/check-inert-authority.sh.
#
# WHY THIS FILE EXISTS
#
# scripts/law-mechanisms-manifest.txt names mechanisms with no call site at
# all. This names the failure next door, which nothing could see: a mechanism
# that IS called, IS handed its authorising witness, and throws it away.
#
#     fn run(&self, cmd: &str, _authority: Authority) -> Result<ShellOutput> {
#         let words = shell_words::split(cmd)?;
#         Command::new(&words[0]).args(&words[1..]).output()      // spawns
#     }
#
# The signature says a caller must hold an `Authority` to get here. The body
# says the `Authority` decides nothing. The gap between those two true
# statements is the finding: the witness carried no TARGET, so an `Authority`
# obtained for one command discharged the gate for any other. The type proved
# that SOME check ran, not that THIS act was checked.
#
# That example is now HISTORY, and deliberately kept: `RealEffects::run` spends
# against the command it is about to spawn (C2.4), so the row that used to head
# this file is class N. `DischargedBundle` carries the subject it was
# discharged for, `Authority::spend_on` compares it, and the mint side takes
# the target so there is something to compare against. What remains open is
# named in the two D rows below, and neither is a spawn.
#
# The rest of the `Act` collapse (ADR 0006) is still signature churn across
# three crates. This file exists so the number cannot grow while that work
# lands, which is why C0 precedes the four collapses at all.
#
# WHAT COUNTS
#
# A parameter whose name starts with `_` and whose type is one of WITNESS
# below, anywhere in the production region (tests/, benches/, examples/,
# fuzz/, *_tests.rs and #[cfg(test)] blocks excluded) of every file
# `git ls-files` reports. The vocabulary lives HERE and not in the Rust, for
# the reason .clippy-ratchet.toml keeps its lint list in the toml: so the gate
# and its documentation cannot drift apart.
#
# WHAT DELIBERATELY DOES NOT COUNT, AND THIS IS THE ONE JUDGEMENT HERE
#
# The `Seal` family -- `Seal`, `CertificateSeal`, `GrantSeal`, `SealedProof` --
# is NOT in WITNESS. Those are private zero-sized types threaded through
# constructors so nothing outside the module can call them (#2450). Thirteen
# sites, and EVERY one is `_`-bound, because being unread is the entire
# mechanism. Listing them would put the tree's most deliberate witnesses at the
# top of a list of accidents and bury the ones that are real. A seal guards
# CONSTRUCTION; this gate is about a guard on ACTION.
#
# WHAT A ROW MEANS
#
#   CLASS | file | scope | count | note
#
# `scope` is the enclosing `impl` target (for `impl T for X`, that is `X`) or
# `trait T`. Not the file, and portcullis-effects is why. Every one of these
# rows but two used to name lib.rs -- 37 of the then-40 sites (the "30 of the
# 40" written here originally did not add up) -- and they were not alike.
# DenyAllEffects refuses every call, RecordingEffects writes a log line, and
# RealEffects::{run, commit, push} spawned processes. A file-level allowance
# would have laundered the three that mattered behind the thirty-four that did
# not, which is exactly the shape of laundering .dead-code-ratchet.toml refuses
# between crates.
#
# Those three are paid now, and the per-scope key is what made it possible to
# say so exactly: the RealEffects row went 6 -> 3, a statement about which
# mechanism changed. A file count going 37 -> 31 would have said nothing.
#
# The key is (file, scope), so a scope that MOVES file reads to this gate as a
# row with no sites left -- and its message says "the debt was paid", which is
# its only hypothesis for that shape and was wrong when the doubles moved to
# doubles.rs. Today lib.rs holds 6 (RealEffects 3, three trait declarations 1
# each) and doubles.rs holds 25 (12 + 7 + 6). If a row ever reports paid,
# confirm the code did not simply move before lowering INERT_TOTAL.
#
#   N -- the scope performs no act, so no authority is being discarded.
#   D -- debt: the act happens and the witness is dropped.
#
# Counts are EXACT IN BOTH DIRECTIONS and INERT_TOTAL must equal their sum. A
# slack pin lets a new site appear inside a scope that already has a row, which
# is the failure this gate exists to catch -- the argument DEAD_COUNT makes
# next door. A row whose scope has no inert site left also FAILS: the debt was
# paid, so delete the row and lower the pin.
#
# SEEDED 2026-09-10 BY THE GATE, not carried in from the audit's hand count.
# The instruction .dead-code-ratchet.toml records -- "re-measure with the gate
# rather than trust the audit's hand-counted 130" -- applied here too, and it
# again earned its keep: a hand-rolled `#[cfg(test)]` stripper written while
# prototyping this gate disagreed with the shared
# `law_mechanisms::production_region` by one site over the same corpus. The
# stripper is now shared rather than restated, so this gate,
# check-law-mechanisms.sh, check-mediation.sh and check-extracted-callsites.sh
# give one answer to "what is production".
#
# Adding a row is not a way to park a witness. A row is a debt with a name.

WITNESS = [
  "Authority",
  "VerifiedAuthority",
  "VerifiedPermissions",
  "CertifiedPermissions",
  "WorkloadCertificate",
  "LaunchAttestation",
  "VerifiedGrant",
  "BindingProof",
  "CheckProof",
  "DecisionToken",
  "DeclassificationToken",
  "SessionCleanseToken",
  "Authorized",
  "GuardedAction",
]

N | crates/portcullis-effects/src/lib.rs | RealEffects | 3 | WAS THE finding, at 6 and class D: run() spawned the parsed command, commit() ran `git add -u` and committed, push() ran `git push` -- each took Authority BY VALUE and dropped it, so the witness bounded nothing about which command, remote or branch. C2.4 paid it: all three now spend against the act they are about to perform (`Authority::spend_on`), and the mint side takes the target so the bundle can bind it. The three left are WebEffect::fetch/search and AgentSpawnEffect::spawn, whose bodies are Err(NotImplemented) -- nothing is performed, so there is no act for an authority to bound, and the group is no longer mixed.
D | crates/nucleus-identity/src/ca/mod.rs | trait CaClient | 1 | sign_attested_csr's DEFAULT body is self.sign_csr(csr, private_key, identity, ttl) with `_attestation: &LaunchAttestation` unread, under a doc comment promising "The attestation is cryptographically bound to the certificate" and "Verifiers can require specific attestation hashes". SelfSignedCa overrides it (ca/self_signed.rs); SpireCaClient (ca/spire.rs) does not, so it inherits the version that binds nothing.
D | crates/nucleus-ifc-kernel/src/ifc_api.rs | FlowTracker | 1 | reset_session_ceiling takes `_token: &SessionCleanseToken` by REFERENCE and never consumes it, then assigns the new ceiling. A cleanse token that survives its own use is not single-use; ADR 0006's C3.2 folds this into the one burn ledger.
N | crates/portcullis-effects/src/doubles.rs | DenyAllEffects | 12 | every method returns Err(PolicyDenied(..)) or Err(io::Error). Nothing is performed, so there is no act for an authority to bound. Dropping the witness here is correct, not deferred.
N | crates/portcullis-effects/src/doubles.rs | RecordingEffects | 7 | each method calls self.record(..) and returns; a test double that performs no effect. Same reasoning as DenyAllEffects. Was 10: run/commit/push now spend, matching RealEffects, because PolicyEnforced stopped spending on their behalf -- a double that dropped the authority where the real one spends would diverge on exactly the property the receipt tests check, and did, for one commit.
N | crates/portcullis-effects/src/doubles.rs | AllowListEffects | 6 | a simulation double: read() returns a canned file_read_response, write()/append() only check_path, glob()/search() return empty. No real I/O. Worth reading anyway as a miniature of the whole finding -- it enforces its OWN allow-list and never consults the authority it was handed.
N | crates/portcullis-effects/src/lib.rs | trait ShellEffect | 1 | run_argv's declaration, no body. The underscore is a naming choice in a signature; implementors bind it themselves and are counted at their own scope.
N | crates/portcullis-effects/src/lib.rs | trait AsyncShellSpawnEffect | 1 | run_argv_async's declaration, no body. As above.
N | crates/portcullis-effects/src/lib.rs | trait NetEffect | 1 | fetch's declaration, no body. As above.
N | crates/nucleus-cli/src/grant.rs | <free> | 1 | verified_line(_verified: &VerifiedGrant) -> &'static str returns a constant string. A display helper takes the type to prove the caller verified something; there is nothing in the value to read.
INERT_TOTAL=34
